[project]
name = "life-coach-agent"
version = "0.1.0"
description = "Life coach agent app"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "openai-agents>=0.0.17",
    "python-dotenv>=1.0.0",
    "streamlit>=1.32.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
